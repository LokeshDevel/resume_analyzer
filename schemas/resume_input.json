{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Resume Input",
  "description": "Schema for resume upload request validation",
  "type": "object",
  "required": ["file"],
  "properties": {
    "file": {
      "type": "string",
      "description": "Path to uploaded resume file (PDF, DOCX, PNG, JPG)"
    },
    "file_type": {
      "type": "string",
      "enum": ["pdf", "docx", "png", "jpg", "jpeg"],
      "description": "File extension / MIME type"
    },
    "job_description": {
      "type": "string",
      "description": "Target job description text to compare against",
      "minLength": 50
    },
    "job_title": {
      "type": "string",
      "description": "Target job title (optional, extracted from JD if missing)"
    },
    "analysis_options": {
      "type": "object",
      "properties": {
        "include_rewrite": {
          "type": "boolean",
          "default": true,
          "description": "Whether to generate rewritten summary and bullet points"
        },
        "industry": {
          "type": "string",
          "enum": ["technology", "business", "healthcare", "finance", "education", "general"],
          "default": "technology",
          "description": "Industry context for scoring calibration"
        }
      }
    }
  }
}
